div
  style(type="text/css"): include:stylus index.styl
  script(type="@plotdb/block"): include:lsc index.ls
  plug.form-group(name="widget",ld="base")
    plug(name="head")
      div(ld="display", data-display="block"): label.d-flex.align-items-end.flex-wrap.justify-content-between
        div
          span(ld="label")
          span.variant.text-sm.ml-2 (#[span(ld="variant")])
          span.text-danger.ml-2(ld="is-required") *
        .m-edit: .mf-note.text-sm(ld="limitation")
      .text-sm.text-muted(ld="display", data-display="block"): div(ld="desc", style="margin:-.6em 0 .7em")
    plug(name="body")
    plug(name="foot")
      .m-edit.notes(ld="display",data-display="block"): .mf-note.text-sm.note(ld-each="note")
